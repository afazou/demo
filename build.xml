<?xml version="1.0" encoding="UTF-8"?>
<project name="APITest" default="setup">
    <property name="basedir" value="." />
    <target name="setup">
        <delete dir="${basedir}/logs" />
        <mkdir dir="${basedir}/aaaa" />
    </target>
</project>